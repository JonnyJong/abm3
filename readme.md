# 番剧管理器 / ABM
正在重建该项目，敬请期待…
The project is being rebuilt, stay tuned...

# 构建 / Build
```bash
npm install -g typescript
npm install -g pug
npm install
pug ./layout/index.pug
tsc
```

# 运行 / Run
```bash
npm start
```
