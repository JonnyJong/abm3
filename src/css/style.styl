@import "var"

*
  box-sizing border-box
body
  margin 0
  color var(--font-color)
  user-select none
  font-size 14px
  height 100vh
  width 100vw
  overflow hidden

.avatar
  border-radius 100%

@import "include/*"